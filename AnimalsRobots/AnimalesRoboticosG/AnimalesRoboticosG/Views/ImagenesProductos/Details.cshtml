@model Conexiones.Animales.Models.ImagenesProductos

@{
    ViewBag.Title = "Detalles";
}

<div class="cotainer">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header text-black-50 bg-light">Detalle de la imágen</div>
                <div class="form-group row">
                    <div class="col-md-6">
                        <strong>Producto:</strong>
                        @Html.DisplayFor(model => model.Productos.nombre)
                    </div>
                </div>
                @*@{
                    foreach (var img in Model.ImagenesProductos)
                    {
                        <img width="300" src="@img.url" />
                    }
                }*@

                <div class="col-md-6 offset-md-4">
                    <button type="button" class="btn btn-warning btn-sm">
                        <i class="fa fa-edit"></i>
                        @Html.ActionLink("Editar", "Edit", new { id = Model.id }) |
                    </button>
                </div>

                @Html.ActionLink("Volver", "Index")
            </div>
        </div>
    </div>
</div>